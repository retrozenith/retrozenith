{
  "name": "retrozenith-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && bun install && bun run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
